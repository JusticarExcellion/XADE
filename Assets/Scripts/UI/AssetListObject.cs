using UnityEngine;
using UnityEngine.UI;

public class
AssetListObject : Selectable
{
    //TODO: Implement selecting something from the asset list when clicked once
    //it is selected, if it is double clicked then the object should be chosen,
    //the window closed, and we should transition to a placement mode for the
    //object
}
